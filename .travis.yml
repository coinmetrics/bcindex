language: java
# whitelist
branches:
  only:
    - index_rel
    - index_rel_production
notifications:
  email:
    on_pull_requests: true
    recipients:
      - BletchleyIndexes@gmail.com
      - froehlich@uwalumni.com
    on_success: always # default: never
    on_failure: always # default: always

script: mvn clean package && ./deploy_all_prod.sh
